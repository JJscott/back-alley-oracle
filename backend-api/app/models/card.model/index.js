const { findAll } = require('./findAll');
const { getOne } = require('./getOne');
const { getRandom } = require('./getRandom');

module.exports = {
  findAll: findAll,
  getOne: getOne,
  getRandom: getRandom
};